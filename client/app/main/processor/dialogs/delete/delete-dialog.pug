doctype html

md-dialog.delete-dialog(aria-label='Delete processors')
    md-toolbar.md-accent.md-hue-2
        .md-toolbar-tools(layout='row', layout-align='space-between center')
            span.title(translate='PROCESSOR.DELETE_PROCESSORS') Delete Processors
            md-button.md-icon-button(
                ng-click='vm.cancel()'
                aria-label='Close dialog'
                tranlate, translate-aria-label='PROCESSOR.CLOSE_DIALOG'
            )
                md-icon(md-font-icon='icon-close')
    md-dialog-content(ms-scroll)
        div.delete-warn-message.md-title
            md-icon.md-warn(md-font-icon='icon-alert-circle')
            span.md-warn(translate='PROCESSOR.DELETE_WARN_MSG') Do you really want to delete the processors?
        md-list(ng-repeat='processor in vm.processors')
            md-list-item
                p.md-subhead {{processor.name}}
                md-checkbox.md-secondary(ng-model='processor.delete')
    md-dialog-actions(layout='row', layout-align='space-between center')
        div(layout='row', layout-align='start center')
            md-button.send-button.md-accent.md-raised(
                ng-click='vm.cancel()'
                aria-label='Cancel'
                translate='PROCESSOR.CANCEL'
                translate-attr-aria-label='PROCESSOR.CANCEL'
            )
                | CANCEL
        div(layout='row')
            md-button.send-button.md-warn.md-raised(
                ng-click='vm.confirm()'
                aria-label='Confirm'
                translate='PROCESSOR.CONFIRM'
                translate-attr-aria-label='PROCESSOR.CONFIRM'
            )
                | CONFIRM


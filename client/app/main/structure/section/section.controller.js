'use strict';

export default class SectionController {
  // Data

  // Methods
  constructor(socket) {
    'ngInject';

    this.socket = socket;
  }

  $onInit() {
  }
}

/* vim:set sw=2 ts=2 sts=2: */
